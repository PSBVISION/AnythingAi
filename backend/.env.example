# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection (Docker)
# Use this for Docker MongoDB:
MONGODB_URI=mongodb://appuser:apppassword123@localhost:27017/anything-ai

# Or use this for MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/anything-ai

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d
